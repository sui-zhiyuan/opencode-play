[build-system]
requires = ["uv_build>=0.11.30,<0.12"]
build-backend = "uv_build"

[project]
name = "daft-demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "daft>=0.7.21",
    "pandas>=2.3.3",
    "ray",
]

[tool.uv.build-backend]
module-root = ""
